import type { Question, QuestionPaper } from '../types'
import { defaultNumbering, formatNumber } from '../utils/numbering'
export function PaperRenderer({paper,questions}:{paper:QuestionPaper;questions:Question[]}){
 const map=new Map(questions.map(q=>[q.id,q])); const ids=paper.questionIds; const rtl=questions.filter(q=>ids.includes(q.id)).filter(q=>q.direction==='rtl').length>ids.length/2
 return <article className="qpg-paper mx-auto bg-white text-black" dir={rtl?'rtl':'ltr'}><header className="qpg-paper-header"><h1>{paper.institutionName}</h1><h2>{paper.examName}</h2><div className="qpg-meta"><span>{paper.className}</span><span>{paper.subject}</span><span>{paper.time}</span><span>Full Marks: {paper.fullMarks}</span></div>{paper.instructions&&<p className="qpg-instructions">{paper.instructions}</p>}</header><div className="qpg-paper-body">{ids.map((id,i)=>{const q=map.get(id);if(!q)return null;return <section key={q.id} className="qpg-question" dir={q.direction} lang={q.language}><div className="qpg-question-line"><strong>{formatNumber(i+1,defaultNumbering(q.language))}</strong><span className="qpg-question-text bidi-isolate">{q.questionText}</span><span className="qpg-marks">[{q.marks}]</span></div>{q.options&&<div className="qpg-options">{q.options.map((o,j)=><div key={o.id} dir={q.direction}><span>{formatNumber(j+1,q.language==='bn'?'bangla-alpha':q.language==='ar'||q.language==='ur'?'arabic-alpha':'alpha')}</span> {o.text}</div>)}</div>}</section>})}</div><footer className="qpg-footer"><span>{paper.institutionName}</span><span className="qpg-page-number">Page <span className="page-number"/></span></footer></article>
}