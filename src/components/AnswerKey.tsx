import { useState } from 'react'
import type { Question, QuestionPaper } from '../types'
import { exportAnswerKeyDocx, exportAnswerKeyPdf } from '../exporters/answerKey'
export function AnswerKey({paper,questions,onClose}:{paper:QuestionPaper;questions:Question[];onClose:()=>void}){
 const mcqs=paper.questionIds.map(id=>questions.find(q=>q.id===id)).filter((q):q is Question=>Boolean(q&&q.correctAnswer!==undefined))
 const [busy,setBusy]=useState(false)
 const run=async(kind:'pdf'|'docx')=>{setBusy(true);try{kind==='pdf'?await exportAnswerKeyPdf(paper,questions,paper.name+' - Answer Key'):await exportAnswerKeyDocx(paper,questions,paper.name+' - Answer Key')}finally{setBusy(false)}}
 return <div className="fixed inset-0 z-[70] grid place-items-center bg-slate-950/50 p-3 backdrop-blur-[2px]"><div className="max-h-[92vh] w-full max-w-3xl overflow-auto rounded-xl border border-slate-200 bg-white shadow-2xl dark:border-slate-700 dark:bg-slate-900"><div className="sticky top-0 z-10 flex items-center justify-between border-b border-slate-200 bg-white px-5 py-4 dark:border-slate-800 dark:bg-slate-900"><h2 className="text-lg font-semibold">Answer Key</h2><button onClick={onClose} className="min-h-10 rounded-lg border px-3 text-sm font-medium hover:bg-slate-50 dark:border-slate-700 dark:hover:bg-slate-800">Close</button></div>{mcqs.length===0?<p className="py-10 text-center text-sm text-slate-500">No questions with correct answers.</p>:<div className="space-y-2 px-5 py-5">{mcqs.map((q,i)=><div key={q.id} className="flex flex-col gap-2 rounded-lg border border-slate-200 p-3 sm:flex-row sm:items-center sm:justify-between dark:border-slate-800"><span>{i+1}. {q.questionText}</span><strong>{Array.isArray(q.correctAnswer)?q.correctAnswer.join(', '):q.correctAnswer}</strong></div>)}</div>}<div className="sticky bottom-0 flex flex-wrap gap-2 border-t border-slate-200 bg-white px-5 py-4 dark:border-slate-800 dark:bg-slate-900"><button disabled={busy||mcqs.length===0} onClick={()=>run('pdf')} className="min-h-11 rounded-lg bg-slate-900 px-4 text-sm font-semibold text-white disabled:opacity-50">Answer Key PDF</button><button disabled={busy||mcqs.length===0} onClick={()=>run('docx')} className="min-h-11 rounded-lg border px-4 text-sm font-medium disabled:opacity-50">Answer Key DOCX</button><button onClick={()=>window.print()} className="min-h-11 rounded-xl border px-4">Print</button></div></div></div>
}