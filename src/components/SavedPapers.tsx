import { Copy, Edit3, FileText, Trash2, KeyRound, Search, Plus, MoreHorizontal } from 'lucide-react'
import { useState } from 'react'
import { useAppStore } from '../stores/appStore'
export function SavedPapers({onCreate,onEdit,onAnswerKey}:{onCreate:()=>void;onEdit?:(id:string)=>void;onAnswerKey?:(id:string)=>void}){
 const papers=useAppStore(s=>s.papers),del=useAppStore(s=>s.deletePaper),add=useAppStore(s=>s.addPaper)
 const [query,setQuery]=useState('')
 const filtered=papers.filter(p=>`${p.name} ${p.subject} ${p.className} ${p.examName}`.toLowerCase().includes(query.toLowerCase()))
 const duplicate=(p:typeof papers[number])=>add({...p,id:crypto.randomUUID(),name:p.name+' Copy',status:'draft',createdAt:new Date().toISOString(),updatedAt:new Date().toISOString()})
 return <section className="space-y-4"><div className="flex flex-col gap-3 border-b border-slate-200 pb-4 sm:flex-row sm:items-end sm:justify-between dark:border-slate-800"><div><h2 className="text-lg font-semibold">Saved Papers</h2><p className="text-sm text-slate-500">{papers.length} saved paper(s)</p></div><button onClick={onCreate} className="inline-flex min-h-10 items-center justify-center gap-2 rounded-lg bg-slate-900 px-4 text-sm font-semibold text-white shadow-sm dark:bg-white dark:text-slate-950"><Plus size={16}/>Create Paper</button></div><div className="rounded-xl border border-slate-200 bg-white p-2 dark:border-slate-800 dark:bg-slate-900"><div className="relative"><Search className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400" size={16}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search saved papers..." className="min-h-10 w-full rounded-lg border border-slate-200 bg-white pl-9 pr-3 text-sm outline-none focus:border-slate-400 dark:border-slate-700 dark:bg-slate-950"/></div></div>{papers.length===0?<div className="grid min-h-[45vh] place-items-center rounded-xl border bg-white dark:bg-slate-900"><div className="text-center"><FileText className="mx-auto text-slate-400"/><p className="mt-3 text-sm text-slate-500">No saved papers</p><button onClick={onCreate} className="mt-3 text-sm font-medium underline">Create your first paper</button></div></div>:filtered.length===0?<div className="rounded-xl border p-10 text-center text-sm text-slate-500">No matching papers found.</div>:<div className="overflow-hidden rounded-xl border border-slate-200 bg-white dark:border-slate-800 dark:bg-slate-900"><div className="hidden grid-cols-[1fr_180px_160px] border-b bg-slate-50 px-4 py-2 text-[11px] font-semibold uppercase tracking-wide text-slate-500 sm:grid dark:bg-slate-950"><span>Paper</span><span>Details</span><span className="text-right">Actions</span></div>{filtered.map(p=><div key={p.id} className="border-b border-slate-100 px-4 py-3 last:border-0 hover:bg-slate-50/70 dark:border-slate-800 dark:hover:bg-slate-950/40"><div className="grid gap-3 sm:grid-cols-[1fr_180px_160px] sm:items-center"><div className="min-w-0"><h3 className="truncate font-semibold">{p.name}</h3><p className="mt-0.5 truncate text-xs text-slate-500">{p.examName || 'Examination'} · {p.subject || 'Subject'} · {p.className || 'Class'}</p></div><div className="flex flex-wrap gap-1.5 text-xs"><span className="rounded-md bg-slate-100 px-2 py-1 dark:bg-slate-800">{p.fullMarks} marks</span><span className="rounded-md bg-slate-100 px-2 py-1 capitalize dark:bg-slate-800">{p.status}</span></div><div className="flex justify-start gap-1 sm:justify-end"><button onClick={()=>onEdit?.(p.id)} className="grid size-9 place-items-center rounded-lg border hover:bg-slate-50 dark:border-slate-700 dark:hover:bg-slate-800" aria-label="Edit"><Edit3 size={15}/></button><button onClick={()=>onAnswerKey?.(p.id)} className="grid size-9 place-items-center rounded-lg border hover:bg-slate-50 dark:border-slate-700 dark:hover:bg-slate-800" aria-label="Answer Key"><KeyRound size={15}/></button><button onClick={()=>duplicate(p)} className="grid size-9 place-items-center rounded-lg border hover:bg-slate-50 dark:border-slate-700 dark:hover:bg-slate-800" aria-label="Duplicate"><Copy size={15}/></button><button onClick={()=>del(p.id)} className="grid size-9 place-items-center rounded-lg border text-red-600 hover:bg-red-50 dark:border-slate-700 dark:hover:bg-red-950/30" aria-label="Delete"><Trash2 size={15}/></button></div></div></div>)}</div>}</section>
}