import { Copy, FileText, Pencil, Trash2 } from 'lucide-react'
import { useAppStore } from '../stores/appStore'
export function SavedPapers({onCreate}:{onCreate:()=>void}){
 const papers=useAppStore(s=>s.papers),del=useAppStore(s=>s.deletePaper),add=useAppStore(s=>s.addPaper)
 return <section className="space-y-5"><div className="flex items-center justify-between"><div><h2 className="text-xl font-bold">Saved Papers</h2><p className="text-sm text-slate-500">{papers.length} saved paper(s)</p></div><button onClick={onCreate} className="min-h-11 rounded-xl bg-slate-950 px-4 text-sm font-medium text-white dark:bg-white dark:text-slate-950">Create Paper</button></div>{papers.length===0?<div className="grid min-h-[45vh] place-items-center rounded-2xl border"><div className="text-center"><FileText className="mx-auto"/><p className="mt-3 text-sm text-slate-500">No saved papers</p><button onClick={onCreate} className="mt-3 underline">Create your first paper</button></div></div>:<div className="space-y-3">{papers.map(p=><div key={p.id} className="rounded-2xl border bg-white p-4 dark:border-slate-800 dark:bg-slate-900"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h3 className="font-semibold">{p.name}</h3><p className="text-sm text-slate-500">{p.subject} · {p.className} · {p.fullMarks} marks · {p.status}</p></div><div className="flex gap-1"><button onClick={()=>add({...p,id:crypto.randomUUID(),name:p.name+' Copy',createdAt:new Date().toISOString(),updatedAt:new Date().toISOString()})} className="grid size-10 place-items-center rounded-lg border" aria-label="Duplicate"><Copy size={16}/></button><button onClick={()=>del(p.id)} className="grid size-10 place-items-center rounded-lg border text-red-600" aria-label="Delete"><Trash2 size={16}/></button></div></div></div>)}</div>}</section>
}